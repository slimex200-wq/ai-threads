# AI 카드뉴스 v2 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 뉴스 소스 확장(9개 피드) + 2단계 필터링(키워드→Claude) + GitHub Actions 매일 자동 생성

**Architecture:** config.py에 피드/키워드 추가 → news_filter.py로 키워드 1차 필터 → ai_writer.py 프롬프트 수정(선별+문구 통합) → cardnews.py 파이프라인 연결 → GitHub Actions 워크플로우

**Tech Stack:** Python 3.11, feedparser, anthropic, html2image, GitHub Actions

**Spec:** `docs/superpowers/specs/2026-03-20-v2-design.md`

---

## File Structure

```
ai-cardnews/
├── config.py              # MODIFY: 피드 5개 추가 + AI_KEYWORDS 리스트
├── news_filter.py         # CREATE: 키워드 필터링 모듈
├── ai_writer.py           # MODIFY: 프롬프트에 선별 로직 통합
├── cardnews.py            # MODIFY: 파이프라인에 필터 단계 삽입
├── rss_collector.py       # (변경 없음)
├── card_renderer.py       # (변경 없음)
├── tests/
│   └── test_news_filter.py  # CREATE
├── .github/workflows/
│   └── daily-cardnews.yml   # CREATE
├── .gitignore               # MODIFY: output/ 관련 규칙
└── output/                  # CREATE: GitHub Actions 결과물 저장
```

---

### Task 1: config.py에 피드 + 키워드 추가

**Files:**
- Modify: `config.py`

- [ ] **Step 1: RSS_FEEDS에 5개 피드 추가**

```python
# config.py RSS_FEEDS 수정
RSS_FEEDS = [
    # 영문
    "https://techcrunch.com/category/artificial-intelligence/feed/",
    "https://www.theverge.com/rss/ai-artificial-intelligence/index.xml",
    "https://www.technologyreview.com/feed/",
    "https://feeds.arstechnica.com/arstechnica/technology-lab",
    "https://openai.com/blog/rss.xml",
    "https://blog.google/technology/ai/rss/",
    "https://huggingface.co/blog/feed.xml",
    # 한국어
    "https://www.aitimes.com/rss/allArticle.xml",
    "https://zdnet.co.kr/rss/ai_news.xml",
]
```

- [ ] **Step 2: AI_KEYWORDS 리스트 추가**

```python
# config.py 하단에 추가
AI_KEYWORDS = [
    # 영문
    "ai", "artificial intelligence", "machine learning", "deep learning",
    "neural network", "llm", "large language model", "gpt", "claude",
    "gemini", "chatbot", "generative ai", "transformer", "diffusion",
    "reinforcement learning", "computer vision", "nlp",
    "natural language processing", "openai", "anthropic", "hugging face",
    # 한국어
    "인공지능", "머신러닝", "딥러닝", "생성형", "대규모 언어 모델",
    "챗봇", "자연어 처리", "컴퓨터 비전", "강화학습",
]
```

- [ ] **Step 3: 테스트 — import 확인**

Run: `python -c "from config import RSS_FEEDS, AI_KEYWORDS; print(len(RSS_FEEDS), len(AI_KEYWORDS))"`
Expected: `9 30` (피드 9개, 키워드 ~30개)

- [ ] **Step 4: 커밋**

```bash
git add config.py
git commit -m "feat: RSS 피드 5개 추가 + AI 키워드 리스트"
```

---

### Task 2: 키워드 필터링 모듈

**Files:**
- Create: `news_filter.py`
- Create: `tests/test_news_filter.py`

- [ ] **Step 1: 테스트 작성**

```python
# tests/test_news_filter.py
from news_filter import filter_by_keywords

def test_matches_title_keyword():
    articles = [
        {"title": "New GPT-5 model released", "summary": "Details about release"},
        {"title": "Best pizza recipes", "summary": "How to make pizza"},
    ]
    result = filter_by_keywords(articles)
    assert len(result) == 1
    assert "GPT" in result[0]["title"]

def test_matches_summary_keyword():
    articles = [
        {"title": "Tech update", "summary": "New artificial intelligence breakthrough"},
    ]
    result = filter_by_keywords(articles)
    assert len(result) == 1

def test_case_insensitive():
    articles = [
        {"title": "machine learning advances", "summary": "details"},
    ]
    result = filter_by_keywords(articles)
    assert len(result) == 1

def test_korean_keyword_match():
    articles = [
        {"title": "인공지능 기술 동향", "summary": "최신 트렌드"},
    ]
    result = filter_by_keywords(articles)
    assert len(result) == 1

def test_no_match_returns_empty():
    articles = [
        {"title": "Weather forecast", "summary": "Rain tomorrow"},
    ]
    result = filter_by_keywords(articles)
    assert len(result) == 0

def test_max_count_limits_results():
    articles = [
        {"title": f"AI news {i}", "summary": "AI content"} for i in range(20)
    ]
    result = filter_by_keywords(articles, max_count=10)
    assert len(result) == 10
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest tests/test_news_filter.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'news_filter'`

- [ ] **Step 3: news_filter.py 구현**

```python
from config import AI_KEYWORDS


def filter_by_keywords(articles, keywords=None, max_count=10):
    keywords = keywords or AI_KEYWORDS
    lower_keywords = [k.lower() for k in keywords]
    matched = []

    for article in articles:
        text = (article.get("title", "") + " " + article.get("summary", "")).lower()
        if any(kw in text for kw in lower_keywords):
            matched.append(article)

    return matched[:max_count]
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest tests/test_news_filter.py -v`
Expected: 6 passed

- [ ] **Step 5: 커밋**

```bash
git add news_filter.py tests/test_news_filter.py
git commit -m "feat: 키워드 기반 뉴스 필터링 모듈"
```

---

### Task 3: ai_writer.py 프롬프트 수정 (선별 + 문구 통합)

**Files:**
- Modify: `ai_writer.py`
- Modify: `tests/test_ai_writer.py`

- [ ] **Step 1: 테스트 추가 — 선별 통합 프롬프트 확인**

```python
# tests/test_ai_writer.py에 추가
def test_build_prompt_includes_selection_instruction():
    articles = [
        {"title": f"AI News {i}", "summary": f"Summary {i}", "source": "Test", "link": "https://example.com"}
        for i in range(8)
    ]
    prompt = build_prompt(articles, select_count=4)
    assert "가장 중요하고 흥미로운" in prompt
    assert "4개" in prompt

def test_build_prompt_without_selection():
    articles = [
        {"title": "AI News", "summary": "Summary", "source": "Test", "link": "https://example.com"}
    ]
    prompt = build_prompt(articles)
    assert "AI News" in prompt
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest tests/test_ai_writer.py -v`
Expected: FAIL — `build_prompt() got an unexpected keyword argument 'select_count'`

- [ ] **Step 3: build_prompt 수정**

```python
def build_prompt(articles, select_count=None):
    articles_text = ""
    for i, a in enumerate(articles, 1):
        articles_text += f"\n### Article {i}\n"
        articles_text += f"Title: {a['title']}\n"
        articles_text += f"Summary: {a['summary']}\n"
        articles_text += f"Source: {a['source']}\n"

    selection_instruction = ""
    if select_count and len(articles) > select_count:
        selection_instruction = f"""
먼저 아래 {len(articles)}개 기사 중 가장 중요하고 흥미로운 {select_count}개를 선별해주세요.
선별 기준: AI 업계에 미치는 영향력, 독자 관심도, 정보의 신선도
선별된 {select_count}개 기사만 카드뉴스로 변환해주세요.
"""

    return f"""{selection_instruction}아래 AI 관련 뉴스 기사들을 한국어 인스타그램 카드뉴스용 문구로 변환해주세요.

각 기사에 대해 다음을 생성해주세요:
- title: 카드 제목 (15자 이내, 한국어)
- subtitle: 부제 (20자 이내, 한국어)
- points: 핵심 포인트 2~3개 (각 30자 이내, 한국어)
- source: 출처명

JSON 형식으로 응답해주세요:
{{
  "cover_title": "AI Weekly",
  "cover_date": "YYYY-MM-DD",
  "cards": [
    {{
      "number": 1,
      "title": "...",
      "subtitle": "...",
      "points": ["...", "..."],
      "source": "..."
    }}
  ],
  "closing_message": "읽어주셔서 감사합니다"
}}

기사들:
{articles_text}"""
```

- [ ] **Step 4: generate_card_content에 select_count 전달**

```python
def generate_card_content(articles, select_count=None):
    client = anthropic.Anthropic(api_key=ANTHROPIC_API_KEY)
    prompt = build_prompt(articles, select_count=select_count)

    message = client.messages.create(
        model=MODEL,
        max_tokens=2000,
        messages=[{"role": "user", "content": prompt}],
    )

    response_text = message.content[0].text
    return parse_response(response_text)
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `python -m pytest tests/test_ai_writer.py -v`
Expected: 4 passed

- [ ] **Step 6: 커밋**

```bash
git add ai_writer.py tests/test_ai_writer.py
git commit -m "feat: Claude 프롬프트에 기사 선별 로직 통합"
```

---

### Task 4: cardnews.py 파이프라인 수정

**Files:**
- Modify: `cardnews.py`

- [ ] **Step 1: 필터 단계 삽입 + 진행률 4단계로 변경**

```python
import argparse
import sys
from config import DEFAULT_COUNT, get_output_dir, ANTHROPIC_API_KEY
from rss_collector import collect_news
from news_filter import filter_by_keywords
from ai_writer import generate_card_content
from card_renderer import render_cover, render_news_card, render_closing


def main():
    parser = argparse.ArgumentParser(description="AI 카드뉴스 생성기")
    parser.add_argument("--count", type=int, default=DEFAULT_COUNT, help="최종 카드 개수 (기본: 4)")
    parser.add_argument("--output", type=str, default=None, help="출력 경로")
    args = parser.parse_args()

    if not ANTHROPIC_API_KEY:
        print("[에러] ANTHROPIC_API_KEY 환경변수를 설정해주세요.")
        sys.exit(1)

    # 1. RSS 뉴스 수집
    print("[1/4] AI 뉴스 수집 중...")
    articles = collect_news(max_count=50)
    if not articles:
        print("[에러] 뉴스를 수집하지 못했습니다.")
        sys.exit(1)
    print(f"  → {len(articles)}개 기사 수집")

    # 2. 키워드 필터링
    print("[2/4] AI 관련 기사 필터링 중...")
    filtered = filter_by_keywords(articles, max_count=10)
    if not filtered:
        print("  → 키워드 매칭 없음, 최신순으로 대체")
        filtered = articles[:10]
    print(f"  → {len(filtered)}개 기사 통과")

    # 3. Claude API로 선별 + 카드 문구 생성
    print("[3/4] 카드 문구 생성 중... (Claude API)")
    content = generate_card_content(filtered, select_count=args.count)
    print(f"  → {len(content['cards'])}개 카드 문구 생성 완료")

    # 4. 이미지 생성
    print("[4/4] 카드 이미지 생성 중...")
    output_dir = get_output_dir(args.output)
    generated = []

    total_cards = len(content["cards"])

    path = render_cover(content["cover_title"], content["cover_date"], output_dir, total_cards)
    generated.append(path)
    print(f"  → 표지: card-01.png")

    for i, card in enumerate(content["cards"], 2):
        path = render_news_card(card, i, output_dir, total_cards)
        generated.append(path)
        print(f"  → 카드 {i}: card-{i:02d}.png")

    closing_num = total_cards + 2
    path = render_closing(content["closing_message"], closing_num, output_dir, total_cards)
    generated.append(path)
    print(f"  → 마무리: card-{closing_num:02d}.png")

    print(f"\n완료! {len(generated)}장의 카드뉴스가 생성되었습니다.")
    print(f"저장 위치: {output_dir}")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 기존 테스트 깨지지 않는지 확인**

Run: `python -m pytest tests/ -v`
Expected: 전체 PASS

- [ ] **Step 3: 커밋**

```bash
git add cardnews.py
git commit -m "feat: 파이프라인에 키워드 필터 + Claude 선별 통합"
```

---

### Task 5: GitHub Actions 워크플로우

**Files:**
- Create: `.github/workflows/daily-cardnews.yml`
- Modify: `.gitignore`

- [ ] **Step 1: .gitignore에 규칙 추가**

`.gitignore` 끝에 추가:
```
# 로컬 실행 결과물 (GitHub Actions 결과만 커밋)
design-preview-*/
__pycache__/
.env
```

`output/` 폴더는 gitignore하지 않음 (Actions에서 커밋 대상)

- [ ] **Step 2: 워크플로우 파일 작성**

```yaml
# .github/workflows/daily-cardnews.yml
name: Daily AI Card News

on:
  schedule:
    - cron: '0 23 * * *'  # 매일 오전 8시 KST (UTC 23:00)
  workflow_dispatch:        # 수동 트리거

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Chrome
        uses: browser-actions/setup-chrome@v1
        with:
          chrome-version: stable

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Install fonts
        run: |
          sudo apt-get update
          sudo apt-get install -y fonts-noto-cjk

      - name: Generate card news
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: python cardnews.py --output output/

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add output/
          DATE=$(date +%Y-%m-%d)
          git diff --staged --quiet || git commit -m "chore: $DATE 카드뉴스 생성"
          git push
```

- [ ] **Step 3: card_renderer.py Chrome 경로 크로스플랫폼 처리**

```python
# card_renderer.py 상단 수정
import shutil
from html2image import Html2Image
from pathlib import Path
from config import CARD_WIDTH, CARD_HEIGHT
from font_css import get_font_css


def _find_chrome():
    """크로스플랫폼 Chrome 경로 탐색"""
    # 환경변수 우선
    import os
    env_path = os.environ.get("CHROME_BIN")
    if env_path:
        return env_path
    # Windows
    win_path = r"C:\Program Files\Google\Chrome\Application\chrome.exe"
    if Path(win_path).exists():
        return win_path
    # Linux/CI
    for name in ["google-chrome", "google-chrome-stable", "chromium-browser", "chromium"]:
        found = shutil.which(name)
        if found:
            return found
    return None


chrome_path = _find_chrome()
hti_kwargs = {"size": (CARD_WIDTH, CARD_HEIGHT)}
if chrome_path:
    hti_kwargs["browser_executable"] = chrome_path

hti = Html2Image(**hti_kwargs)
```

- [ ] **Step 4: 커밋**

```bash
git add .github/workflows/daily-cardnews.yml .gitignore card_renderer.py
git commit -m "feat: GitHub Actions 매일 자동 생성 워크플로우"
```

---

### Task 6: 통합 테스트 + 전체 검증

- [ ] **Step 1: 전체 테스트 실행**

Run: `python -m pytest tests/ -v`
Expected: 모든 테스트 PASS

- [ ] **Step 2: 로컬 전체 실행 테스트**

Run: `python cardnews.py --count 3`
Expected: 바탕화면에 카드뉴스 생성, 4단계 진행률 표시

- [ ] **Step 3: GitHub에 푸시 + Actions 수동 트리거 테스트**

```bash
git push origin main
```

GitHub Actions 탭에서 "Run workflow" 클릭하여 수동 실행 확인

- [ ] **Step 4: 최종 커밋**

```bash
git add -A
git commit -m "feat: AI 카드뉴스 v2 완성 — 소스 확장 + 필터링 + 자동화"
```
